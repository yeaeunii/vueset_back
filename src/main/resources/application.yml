spring:
  application:
    name: Vue

  datasource:
    url: jdbc:postgresql://localhost:5432/tmsp
    username: tms_db
    password: tms1234
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081
